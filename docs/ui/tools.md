请帮我设计一下这个工具页，需求如下：
- 需要支持工具包上传的入口，上传内容限定为 `.zip` 格式的文件，且要求里面至少包含两个文件 `README.md` `{tool_name}.tool.js` 文件，不过这是上传校验的逻辑，但是上传时不符合规范的话会报错
- 其余主要展示工具内容的卡片信息如下，请设计一个效果可以将信息完全展示出来
```json
{
	"id": "chrome-devtools",
	"name": "Chrome DevTools MCP",
	"description": "基于 chrome-devtools-mcp 的浏览器自动化工具，完全复用官方实现。支持页面导航、元素操作、性能分析、网络监控、控制台监控等功能。支持 keepAlive 参数保持浏览器状态，便于连续操作和调试。",
	"version": "1.0.0",
	"category": "utility",
	"author": "Prompt Manager",
	"tags": [
		"browser",
		"debugging"
	],
	"scenarios": [
		"网页自动化操作"
	],
	"limitations": [
		"仅支持 Chrome/Chromium 浏览器"
	]
}
```
- 工具卡片点击直接到达工具的说明页面，但这里会先判断该工具的目录下是否存在 `README.md` 文件，无文件则提示无说明，有则跳转到工具介绍页，这个页面会以 markdown 的格式展示文档，同时也支持 流程图、时序图 等的渲染，以便更好的为用户介绍工具
- 工具卡片还可以按照 类别、标签、作者 聚合展示，而且每个聚合效果都要有些差异可以区分出是哪个维度的聚合，尤其是作者页，这里要突出下作者，把他的头像、个人介绍、主页都应该展示出来，同时在列表页还支持按照名称、描述检索
- 数据中的 scenarios、limitations 这两个字段不一定都存在，故在设计的时候需要考虑到没有字段、只存在一个或两个都存在的情况，让页面效果看起来更协调

重要：
1、设计时要遵循当前已完成的页面效果，确保新设计的页面与整体效果的兼容性，且简洁美观大气
2、如有必要可以借助 chrome-devtools 工具对生成的页面效果进行检查，确保交付效果符合预期
3、程序运行命令，在 admin-ui 目录下 执行 `npm  run dev`