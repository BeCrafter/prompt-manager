# 环境变量配置示例
# 复制此文件为 .env 并根据需要修改
SERVER_PORT=5621

# Prompts目录路径 (默认: ~/.prompt-manager/prompts，可通过命令行覆盖)
#PROMPTS_DIR=$HOME/.prompt-manager/prompts

# MCP服务器配置
MCP_SERVER_NAME=prompt-server
MCP_SERVER_VERSION=0.0.7

# 日志级别 (error, warn, info, debug)
LOG_LEVEL=info

# 最大prompt数量限制
MAX_PROMPTS=1000
